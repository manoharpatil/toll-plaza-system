akka {
  loglevel = "info"
  actor {
    provider = "local"
  }
  kafka {
    producer {
      # Define Kafka producer settings if needed
    }
    consumer {
      # Define Kafka consumer settings if needed
    }
  }
}
